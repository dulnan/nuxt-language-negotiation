{
  "name": "playground-module-install",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "typecheck": "vue-tsc --noEmit -p .nuxt/tsconfig.app.json && vue-tsc --noEmit -p .nuxt/tsconfig.server.json"
  },
  "dependencies": {
    "nuxt": "^4.4.2",
    "nuxt-language-negotiation": "file:..",
    "vue": "^3.5.30",
    "vue-router": "^5.0.3"
  }
}
